#include <bits/stdc++.h>
using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    int a=1342,b=23652453;
    while(a+b){
        cin>>a>>b;
        if(a+b)cout<<a+b<<"\n";
    }
}